# Gitignored files to copy into Claude Code worktrees (`claude --worktree` subagent isolation, desktop parallel sessions).
# See https://code.claude.com/docs/en/worktrees#copy-gitignored-files-into-worktrees
#
# Note: when `.claude/settings.json` defines a WorktreeCreate hook, Claude skips
# this file and the hook must copy envs itself (our hook calls
# `.agents/skills/wt/scripts/setup-worktree-unix.sh`, which does).

apps/spa/.env.dev
apps/spa/.env.prod
apps/spa/.env.local
apps/web/.env.dev
apps/web/.env.prod
apps/web/.env.local
apps/expo/.env.local
