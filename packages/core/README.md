# @workspace/core

This package is used to share the same core logic across all apps and packages. This package supposed to be isomorphic, meaning it can be used in browser, server, and react native.
