# Bot authors

Logins (exact match, case-insensitive) treated as the PR review bot. Edit this list for your project.

```
# cursor[bot]
# claude[bot]
# codex[bot]
# github-actions[bot]
```

Lines starting with `#` are ignored. One login per line. GitHub App bots use the `[bot]` suffix.
