# Expo

Expo / React Native mobile app.

## Language

**Device Locale**: The OS-preferred Locale used once at Translation Provider init when no manual choice is active. Mapped by primary language subtag (`en*` → `en-us`, `id*` → `id-id`); otherwise `en-us`. _Avoid_: system language sync on AppState resume
